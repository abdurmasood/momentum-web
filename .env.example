# Marketing Site Configuration
# Copy this file to .env and update with your actual values

# NextAuth Configuration
NEXTAUTH_SECRET=generate-random-string-32-chars-minimum
NEXTAUTH_URL=http://localhost:3000

# Google OAuth - Get from Google Cloud Console
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# JWT Secret for custom token generation
JWT_SECRET=your-jwt-secret-key

# Dashboard URL
NEXT_PUBLIC_DASHBOARD_URL=http://localhost:3001

# Neon Database
# Get from Neon dashboard: https://console.neon.tech
# Pooled connection - for Prisma Client (app queries)
DATABASE_URL="postgresql://user:password@host-pooler.region.aws.neon.tech/dbname?sslmode=require"

# Direct connection - for Prisma Migrate (no pooler)
DIRECT_URL="postgresql://user:password@host.region.aws.neon.tech/dbname?sslmode=require"

# Analytics and Tracking
# These IDs change between development, staging, and production environments
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
NEXT_PUBLIC_GTM_ID=GTM-XXXXXXX
NEXT_PUBLIC_MIXPANEL_TOKEN=your_mixpanel_token_here

# Note: Social media links are constants in /src/constants/marketing.ts
# Update them directly in the constants file
